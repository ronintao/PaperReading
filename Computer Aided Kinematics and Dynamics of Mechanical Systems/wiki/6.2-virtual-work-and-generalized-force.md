# 6.2 虚功与广义力（Virtual Work and Generalized Force）

> 来源：*Computer Aided Kinematics and Dynamics of Mechanical Systems, Vol. 1*，第 213–218 页（§6.2）。
> 本笔记对原文公式逐式展开推导，补全原书略去的中间步骤。

---

## 0. 预备记号（来自 §6.1）

为便于后文推导，先复述本节会用到的几个约定：

- 平面刚体 $i$ 的位形坐标：

$$
\mathbf{q}_i=\begin{bmatrix}\mathbf{r}_i\\[2pt]\phi_i\end{bmatrix},\qquad
\mathbf{r}_i=\begin{bmatrix}x_i\\ y_i\end{bmatrix}
$$

- 旋转矩阵与其对角度的导数：

$$
\mathbf{A}_i=\begin{bmatrix}\cos\phi_i & -\sin\phi_i\\[2pt] \sin\phi_i & \cos\phi_i\end{bmatrix},\qquad
\mathbf{R}=\begin{bmatrix}0 & -1\\[2pt] 1 & 0\end{bmatrix}
$$

- 关键恒等式（90° 旋转矩阵 $\mathbf{R}$ 把"对角度求导"变成"左乘 $\mathbf{R}$"）：

$$
\frac{\partial \mathbf{A}_i}{\partial \phi_i}=\mathbf{A}_i\mathbf{R}\equiv \mathbf{B}_i
$$

  这一点可直接验证：

$$
\frac{\partial \mathbf{A}_i}{\partial \phi_i}
=\begin{bmatrix}-\sin\phi_i & -\cos\phi_i\\ \cos\phi_i & -\sin\phi_i\end{bmatrix}
=\begin{bmatrix}\cos\phi_i & -\sin\phi_i\\ \sin\phi_i & \cos\phi_i\end{bmatrix}
\begin{bmatrix}0 & -1\\ 1 & 0\end{bmatrix}
=\mathbf{A}_i\mathbf{R}
$$

  因此 $\mathbf{B}_i=\mathbf{A}_i\mathbf{R}$（即原书 Eq. 6.1.8 定义的 $\mathbf{B}_i$）。

- 物体上某点 $P$ 在体坐标系下的常向量记为 $\mathbf{s}'^{P}$（在体系中固定，不随时间变化）。

---

## 1. 虚功与广义力的基本定义

作用在体坐标系 $x'\text{-}y'$ 原点的力 $\mathbf{F}$ 与作用在刚体上的力矩 $n$，其**虚功**写成虚位移 $\delta\mathbf{r}$ 与虚转动 $\delta\phi$ 的形式：

$$
\delta W=\delta\mathbf{r}^{T}\mathbf{F}+\delta\phi\, n
\equiv \delta\mathbf{q}^{T}\mathbf{Q}
\tag{6.2.1}
$$

其中

$$
\mathbf{q}=\begin{bmatrix}\mathbf{r}^{T}, & \phi\end{bmatrix}^{T},\qquad
\mathbf{Q}=\begin{bmatrix}\mathbf{F}^{T}, & n\end{bmatrix}^{T}
\tag{6.2.2}
$$

**定义广义力的核心思想**：先把一组力与力矩的虚功写成"物理虚位移/虚转动 × 物理力/力矩"的乘积；再把物理虚位移、虚转动用广义坐标的变分 $\delta\mathbf{q}$ 表示；最后把每个广义坐标变分的系数收集出来——这些系数就被定义为与对应广义坐标关联的**广义力**。**虚功是定义广义力时被保持不变的关键量。**

---

## 2. 一般作用力 $\mathbf{F}^{P}$ 的广义力（Eq. 6.2.3）

考虑作用于刚体 $i$ 上点 $P$ 的力 $\mathbf{F}^{P}$（图 6.2.1）。

**第 1 步：点 $P$ 的全局位置。** 点 $P$ 的位置由刚体质心位置加上旋转后的体内偏置给出：

$$
\mathbf{r}_i^{P}=\mathbf{r}_i+\mathbf{A}_i\mathbf{s}'^{P}
$$

**第 2 步：求点 $P$ 的虚位移。** 对上式取变分。注意 $\mathbf{s}'^{P}$ 在体系内是常量，只有 $\mathbf{r}_i$ 与 $\mathbf{A}_i$（通过 $\phi_i$）变化：

$$
\delta\mathbf{r}_i^{P}
=\delta\mathbf{r}_i+\delta(\mathbf{A}_i)\,\mathbf{s}'^{P}
=\delta\mathbf{r}_i+\frac{\partial \mathbf{A}_i}{\partial \phi_i}\,\delta\phi_i\,\mathbf{s}'^{P}
=\delta\mathbf{r}_i+\delta\phi_i\,\mathbf{B}_i\mathbf{s}'^{P}
$$

这里用到了第 0 节的恒等式 $\partial\mathbf{A}_i/\partial\phi_i=\mathbf{B}_i$。即

$$
\delta\mathbf{r}_i^{P}=\delta\mathbf{r}_i+\delta\phi_i\,\mathbf{B}_i\mathbf{s}'^{P}
$$

**第 3 步：写出力 $\mathbf{F}^{P}$ 的虚功。** 力的虚功是力与作用点虚位移的点积：

$$
\delta W=\delta\mathbf{r}_i^{P\,T}\mathbf{F}^{P}
$$

把第 2 步结果代入并转置展开（注意 $(\delta\mathbf{r}_i+\delta\phi_i\,\mathbf{B}_i\mathbf{s}'^{P})^{T}=\delta\mathbf{r}_i^{T}+\delta\phi_i\,\mathbf{s}'^{P\,T}\mathbf{B}_i^{T}$，其中标量 $\delta\phi_i$ 可自由移动）：

$$
\delta W
=\bigl(\delta\mathbf{r}_i^{T}+\delta\phi_i\,\mathbf{s}'^{P\,T}\mathbf{B}_i^{T}\bigr)\mathbf{F}^{P}
=\delta\mathbf{r}_i^{T}\mathbf{F}^{P}+\delta\phi_i\,\mathbf{s}'^{P\,T}\mathbf{B}_i^{T}\mathbf{F}^{P}
$$

**第 4 步：收集广义坐标变分的系数。** 把上式与 $\delta W=\delta\mathbf{q}_i^{T}\mathbf{Q}=\delta\mathbf{r}_i^{T}(\,\cdot\,)+\delta\phi_i(\,\cdot\,)$ 对照：

- $\delta\mathbf{r}_i$ 的系数为 $\mathbf{F}^{P}$；
- $\delta\phi_i$ 的系数为 $\mathbf{s}'^{P\,T}\mathbf{B}_i^{T}\mathbf{F}^{P}$（标量）。

于是作用于刚体 $i$ 点 $P$ 的一般力对应的广义力为：

$$
\boxed{\;\mathbf{Q}=\begin{bmatrix}\mathbf{F}^{P}\\[4pt]\mathbf{s}'^{P\,T}\mathbf{B}_i^{T}\mathbf{F}^{P}\end{bmatrix}\;}
\tag{6.2.3}
$$

> **物理意义**：$\mathbf{Q}$ 的第三个分量 $\mathbf{s}'^{P\,T}\mathbf{B}_i^{T}\mathbf{F}^{P}$ 正是 $\mathbf{F}^{P}$ 关于体坐标系 $x_i'\text{-}y_i'$ 原点的力矩（与 Eq. 6.1.17 一致）。

---

## 3. 体固连力（Body-fixed Force）的广义力（Eq. 6.2.4）

若力 $\mathbf{F}^{P}$ **随刚体 $i$ 一起转动**（例如固定在航天器上的火箭推进器），则它在体系中是常向量 $\mathbf{F}'^{P}$，全局表达为

$$
\mathbf{F}^{P}=\mathbf{A}_i\mathbf{F}'^{P}
$$

**第 1 步：把 $\mathbf{F}^{P}=\mathbf{A}_i\mathbf{F}'^{P}$ 代入 Eq. 6.2.3：**

$$
\mathbf{Q}=\begin{bmatrix}\mathbf{A}_i\mathbf{F}'^{P}\\[4pt]\mathbf{s}'^{P\,T}\mathbf{B}_i^{T}\mathbf{A}_i\mathbf{F}'^{P}\end{bmatrix}
$$

**第 2 步：化简第三分量中的 $\mathbf{B}_i^{T}\mathbf{A}_i$。** 利用 $\mathbf{B}_i=\mathbf{A}_i\mathbf{R}$ 与旋转矩阵正交性 $\mathbf{A}_i^{T}\mathbf{A}_i=\mathbf{I}$：

$$
\mathbf{s}'^{P\,T}\mathbf{B}_i^{T}\mathbf{A}_i
=\mathbf{s}'^{P\,T}(\mathbf{A}_i\mathbf{R})^{T}\mathbf{A}_i
=\mathbf{s}'^{P\,T}\mathbf{R}^{T}\mathbf{A}_i^{T}\mathbf{A}_i
=\mathbf{s}'^{P\,T}\mathbf{R}^{T}
$$

进一步，$\mathbf{s}'^{P\,T}\mathbf{R}^{T}=(\mathbf{R}\,\mathbf{s}'^{P})^{T}$。因此第三分量为 $(\mathbf{R}\,\mathbf{s}'^{P})^{T}\mathbf{F}'^{P}$。

**第 3 步：把公共因子 $\mathbf{F}'^{P}$ 提到右侧，得到体固连力的广义力：**

$$
\boxed{\;\mathbf{Q}=\begin{bmatrix}\mathbf{A}_i\\[4pt](\mathbf{R}\,\mathbf{s}'^{P})^{T}\end{bmatrix}\mathbf{F}'^{P}\;}
\tag{6.2.4}
$$

> 推导中所用关系：$\mathbf{B}_i=\mathbf{A}_i\mathbf{R}$，故 $\mathbf{s}'^{P\,T}\mathbf{B}_i^{T}\mathbf{A}_i=\mathbf{s}'^{P\,T}\mathbf{R}^{T}=(\mathbf{R}\,\mathbf{s}'^{P})^{T}$。

---

## 4. 单刚体的变分运动方程（Eq. 6.2.5）

采用 Eqs. 6.2.2 / 6.2.3 / 6.2.4 的记号 $\delta\mathbf{q}=[\delta\mathbf{r}^{T},\ \delta\phi]^{T}$，并定义质量矩阵

$$
\mathbf{M}=\operatorname{diag}(m,\ m,\ J')
$$

则 Eq. 6.1.18 的变分运动方程可写成紧凑形式：

$$
\boxed{\;\delta\mathbf{q}^{T}\bigl[\mathbf{M}\ddot{\mathbf{q}}-\mathbf{Q}\bigr]=0\;}
\tag{6.2.5}
$$

对所有与约束相容的 $\delta\mathbf{q}$ 都成立。

---

## 5. 平动弹簧–阻尼–作动器（Translational Spring–Damper–Actuator）

机械系统中常见的力元件，是连接刚体 $i$ 上点 $P_i$ 与刚体 $j$ 上点 $P_j$ 的**柔顺连接**（图 6.2.2），可以是弹簧、阻尼器或液压作动器，沿 $P_i P_j$ 连线方向施力，称为**平动弹簧–阻尼–作动器**。

由于该元件对两刚体施加**等大反向**的力，不再分别对每个刚体计算广义力，而是直接利用虚功定义更为方便。

### 5.1 元件向量与长度（Eqs. 6.2.6–6.2.7）

从 $P_i$ 指向 $P_j$ 的向量：

$$
\mathbf{d}_{ij}=\mathbf{r}_j+\mathbf{A}_j\mathbf{s}'_j-\mathbf{r}_i-\mathbf{A}_i\mathbf{s}'_i
\tag{6.2.6}
$$

元件长度（的平方）由内积给出：

$$
\ell^{2}=\mathbf{d}_{ij}^{T}\mathbf{d}_{ij}
\tag{6.2.7}
$$

> $\ell$ 可正可负，取决于装配时的相对取向。由于力元件通常设计成长度永不为零，应建模以排除病态情形 $\ell=0$；建议采用约定 $\ell>0$。

### 5.2 长度变化率 $\dot\ell$（Eq. 6.2.8）

**第 1 步：对 Eq. 6.2.7 两边求时间导数。** 左边 $\dfrac{d}{dt}(\ell^{2})=2\ell\dot\ell$；右边 $\dfrac{d}{dt}(\mathbf{d}_{ij}^{T}\mathbf{d}_{ij})=\dot{\mathbf{d}}_{ij}^{T}\mathbf{d}_{ij}+\mathbf{d}_{ij}^{T}\dot{\mathbf{d}}_{ij}=2\,\mathbf{d}_{ij}^{T}\dot{\mathbf{d}}_{ij}$（标量转置相等）。故

$$
2\ell\dot\ell=2\,\mathbf{d}_{ij}^{T}\dot{\mathbf{d}}_{ij}
$$

**第 2 步：对 Eq. 6.2.6 求时间导数。** $\mathbf{s}'_i,\mathbf{s}'_j$ 在体系内是常量，对 $\mathbf{A}_i\mathbf{s}'_i$ 求导用链式法则 $\dfrac{d}{dt}(\mathbf{A}_i\mathbf{s}'_i)=\dfrac{\partial\mathbf{A}_i}{\partial\phi_i}\dot\phi_i\,\mathbf{s}'_i=\mathbf{B}_i\mathbf{s}'_i\dot\phi_i$：

$$
\dot{\mathbf{d}}_{ij}=\dot{\mathbf{r}}_j+\mathbf{B}_j\mathbf{s}'_j\dot\phi_j-\dot{\mathbf{r}}_i-\mathbf{B}_i\mathbf{s}'_i\dot\phi_i
$$

**第 3 步：当 $\ell\neq 0$ 时，对第 1 步两边除以 $2\ell$，再代入 $\dot{\mathbf{d}}_{ij}$：**

$$
\dot\ell=\frac{1}{\ell}\,\mathbf{d}_{ij}^{T}\dot{\mathbf{d}}_{ij}
=\Bigl(\frac{\mathbf{d}_{ij}}{\ell}\Bigr)^{T}
\bigl(\dot{\mathbf{r}}_j+\mathbf{B}_j\mathbf{s}'_j\dot\phi_j-\dot{\mathbf{r}}_i-\mathbf{B}_i\mathbf{s}'_i\dot\phi_i\bigr)
\tag{6.2.8}
$$

### 5.3 $\ell\to 0$ 的极限处理（Eq. 6.2.9）

当 $\ell\to 0$ 时，Eq. 6.2.8 右边出现 $\mathbf{d}_{ij}/\ell\to \mathbf{0}/0$ 的不定型。对该比值用洛必达法则（分子分母分别对时间求导）：

$$
\lim_{\ell\to 0}\frac{\mathbf{d}_{ij}}{\ell}
=\lim_{\ell\to 0}\frac{\dot{\mathbf{d}}_{ij}}{\dot\ell}
=\left.\frac{\dot{\mathbf{d}}_{ij}}{\dot\ell}\right|_{\ell=0}
$$

前提是 $\ell=0$ 时 $\dot\ell\neq 0$。把该极限代回 Eq. 6.2.8（即在 $\ell=0$ 处用 $\mathbf{d}_{ij}/\ell\to\dot{\mathbf{d}}_{ij}/\dot\ell$ 替换）：

$$
\dot\ell=\Bigl(\frac{\dot{\mathbf{d}}_{ij}}{\dot\ell}\Bigr)^{T}\dot{\mathbf{d}}_{ij}
=\frac{1}{\dot\ell}\,\dot{\mathbf{d}}_{ij}^{T}\dot{\mathbf{d}}_{ij}
$$

两边乘以 $\dot\ell$，解出 $(\dot\ell)^{2}$：

$$
(\dot\ell)^{2}=\dot{\mathbf{d}}_{ij}^{T}\dot{\mathbf{d}}_{ij}
\tag{6.2.9}
$$

（适用于 $\ell=0$ 时。）$\dot\ell$ 的符号取 $\ell=0$ 发生前一刻的值。

### 5.4 元件内力（Eq. 6.2.10）

规定**张力为正**（即把两刚体拉拢的力为正）。元件内力写为

$$
f=k(\ell-\ell_0)+c\dot\ell+F(\ell,\dot\ell,t)
\tag{6.2.10}
$$

其中 $k$ 为弹簧系数，$\ell_0$ 为弹簧自由长度，$c$ 为阻尼系数，$F$ 为一般作动器力（可依赖 $\ell$、$\dot\ell$ 或时间 $t$）。

### 5.5 虚功（Eqs. 6.2.11–6.2.13）

**虚功的符号约定**：正的 $\delta\ell$ 使两刚体分离，而 $f>0$ 对应张力（把刚体拉拢）。当 $f$ 与 $\delta\ell$ 同为正时，元件对刚体做**负功**。故

$$
\delta W=-f\,\delta\ell
\tag{6.2.11}
$$

**第 1 步：求 $\delta\ell$。** 对 Eq. 6.2.7 取微分（步骤与 $\dot\ell$ 推导相同，把"$d/dt$"换成"$\delta$"）：

$$
2\ell\,\delta\ell=2\,\mathbf{d}_{ij}^{T}\delta\mathbf{d}_{ij}
\;\Longrightarrow\;
\delta\ell=\frac{1}{\ell}\,\mathbf{d}_{ij}^{T}\delta\mathbf{d}_{ij}
$$

其中对 Eq. 6.2.6 取变分（$\delta(\mathbf{A}_i\mathbf{s}'_i)=\mathbf{B}_i\mathbf{s}'_i\,\delta\phi_i$）：

$$
\delta\mathbf{d}_{ij}=\delta\mathbf{r}_j+\mathbf{B}_j\mathbf{s}'_j\,\delta\phi_j-\delta\mathbf{r}_i-\mathbf{B}_i\mathbf{s}'_i\,\delta\phi_i
$$

合并得：

$$
\delta\ell=\Bigl(\frac{\mathbf{d}_{ij}}{\ell}\Bigr)^{T}
\bigl(\delta\mathbf{r}_j+\mathbf{B}_j\mathbf{s}'_j\,\delta\phi_j-\delta\mathbf{r}_i-\mathbf{B}_i\mathbf{s}'_i\,\delta\phi_i\bigr)
\tag{6.2.12}
$$

（$\mathbf{B}_i$ 见 Eq. 6.1.8。）

**第 2 步：代入 Eq. 6.2.11。** 把 Eq. 6.2.12 代入 $\delta W=-f\,\delta\ell$：

$$
\delta W=-\frac{f}{\ell}\,\mathbf{d}_{ij}^{T}
\bigl(\delta\mathbf{r}_j+\mathbf{B}_j\mathbf{s}'_j\,\delta\phi_j-\delta\mathbf{r}_i-\mathbf{B}_i\mathbf{s}'_i\,\delta\phi_i\bigr)
\tag{6.2.13}
$$

### 5.6 广义力（Eqs. 6.2.14–6.2.15）

由定义，作用在刚体 $i$、$j$ 上的广义力就是 Eq. 6.2.13 中各广义坐标变分的系数。

**逐项收集系数。** 展开 Eq. 6.2.13：

$$
\delta W=-\frac{f}{\ell}\Bigl[
\underbrace{\mathbf{d}_{ij}^{T}\delta\mathbf{r}_j}_{\delta\mathbf{r}_j}
+\underbrace{\mathbf{d}_{ij}^{T}\mathbf{B}_j\mathbf{s}'_j\,\delta\phi_j}_{\delta\phi_j}
-\underbrace{\mathbf{d}_{ij}^{T}\delta\mathbf{r}_i}_{\delta\mathbf{r}_i}
-\underbrace{\mathbf{d}_{ij}^{T}\mathbf{B}_i\mathbf{s}'_i\,\delta\phi_i}_{\delta\phi_i}
\Bigr]
$$

- $\delta\mathbf{r}_i$ 的系数（注意 $\mathbf{d}_{ij}^{T}\delta\mathbf{r}_i$ 前带负号，整体再乘 $-f/\ell$）给出 $\mathbf{r}_i$ 方向广义力 $+\dfrac{f}{\ell}\mathbf{d}_{ij}$；
- $\delta\phi_i$ 的系数给出 $\phi_i$ 方向广义力 $+\dfrac{f}{\ell}\mathbf{d}_{ij}^{T}\mathbf{B}_i\mathbf{s}'_i$。

故刚体 $i$ 的广义力：

$$
\mathbf{Q}_i=\frac{f}{\ell}\begin{bmatrix}\mathbf{d}_{ij}\\[4pt]\mathbf{d}_{ij}^{T}\mathbf{B}_i\mathbf{s}'_i\end{bmatrix}
\tag{6.2.14a}
$$

同理，$\delta\mathbf{r}_j$、$\delta\phi_j$ 的系数前为正号、整体乘 $-f/\ell$，给出刚体 $j$ 的广义力：

$$
\mathbf{Q}_j=-\frac{f}{\ell}\begin{bmatrix}\mathbf{d}_{ij}\\[4pt]\mathbf{d}_{ij}^{T}\mathbf{B}_j\mathbf{s}'_j\end{bmatrix}
\tag{6.2.14b}
$$

> 注意 $\mathbf{Q}_i$ 与 $\mathbf{Q}_j$ 的平动部分符号相反（$\pm\frac{f}{\ell}\mathbf{d}_{ij}$），体现等大反向的内力特性。

**$\ell\to 0$ 的极限形式。** 当 $\ell\to 0$ 时 Eq. 6.2.14 出现 $\mathbf{d}_{ij}/\ell$ 的不定型，对 $\ell\to 0$ 用洛必达法则（同 §5.3，$\mathbf{d}_{ij}/\ell\to\dot{\mathbf{d}}_{ij}/\dot\ell$，再用 $\ell=0$ 时 $\mathbf{d}_{ij}=\mathbf{0}$）得：

$$
\mathbf{Q}_i=\frac{f}{\ell}\begin{bmatrix}\dot{\mathbf{d}}_{ij}\\[4pt]\dot{\mathbf{d}}_{ij}^{T}\mathbf{B}_i\mathbf{s}'_i\end{bmatrix},
\qquad
\mathbf{Q}_j=-\frac{f}{\ell}\begin{bmatrix}\dot{\mathbf{d}}_{ij}\\[4pt]\dot{\mathbf{d}}_{ij}^{T}\mathbf{B}_j\mathbf{s}'_j\end{bmatrix},
\qquad \ell=0
\tag{6.2.15}
$$

成立条件为 $\dot\ell\neq 0$，且使用了 $\ell=0$ 时 $\mathbf{d}_{ij}=\mathbf{0}$ 的事实。

---

## 6. 旋转弹簧–阻尼–作动器（Rotational Spring–Damper–Actuator）

考虑图 6.2.3 中的一对刚体：点 $P$ 处定义一个转动副（revolute joint），并有一个**扭转柔顺元件**作用在体固连轴 $x_i'$ 与 $x_j'$ 之间。该元件对刚体 $i$、$j$ 施加**等大反向**的力矩，称为**旋转弹簧–阻尼–作动器**。正力矩定义为：对刚体 $i$ 逆时针、对刚体 $j$ 顺时针作用。

### 6.1 相对转角及其导数（Eqs. 6.2.16–6.2.17）

从 $x_i'$ 轴到 $x_j'$ 轴的夹角 $\theta_{ij}$（逆时针为正）：

$$
\theta_{ij}=\phi_j-\phi_i
\tag{6.2.16}
$$

对时间求导：

$$
\dot\theta_{ij}=\dot\phi_j-\dot\phi_i
\tag{6.2.17}
$$

### 6.2 元件力矩（Eq. 6.2.18）

作用在两刚体间的力矩：

$$
n=k_\theta(\theta_{ij}-\theta_0)+c_\theta\dot\theta_{ij}+N(\theta_{ij},\dot\theta_{ij},t)
\tag{6.2.18}
$$

其中 $k_\theta$ 为扭转弹簧系数，$\theta_0$ 为弹簧自由角，$c_\theta$ 为扭转阻尼系数，$N$ 为一般作动器力矩（可依赖 $\theta_{ij}$、$\dot\theta_{ij}$、时间 $t$）。

### 6.3 虚功与广义力（Eqs. 6.2.19–6.2.22）

**虚功的符号约定**：力矩 $n$ 的虚功是它与相对虚转动 $\delta\theta_{ij}$ 乘积的**负值**——因为当 $\theta_{ij}>0$ 时，正力矩使两轴靠拢，而正的 $\delta\theta_{ij}$ 使其分离：

$$
\delta W=-n\,\delta\theta_{ij}
\tag{6.2.19}
$$

**第 1 步：相对虚转动。** 由 Eq. 6.2.16 取变分：

$$
\delta\theta_{ij}=\delta\phi_j-\delta\phi_i
\tag{6.2.20}
$$

**第 2 步：代入 Eq. 6.2.19：**

$$
\delta W=-n(\delta\phi_j-\delta\phi_i)
\tag{6.2.21}
$$

**第 3 步：收集系数得广义力。** 把 Eq. 6.2.21 展开为 $\delta W=(+n)\,\delta\phi_i+(-n)\,\delta\phi_j$。由于扭转元件只产生力矩、不产生平动力，故 $\mathbf{r}_i,\mathbf{r}_j$ 方向的广义力分量为零，只有转角分量非零：

$$
\mathbf{Q}_i=\begin{bmatrix}0\\ 0\\ n\end{bmatrix},
\qquad
\mathbf{Q}_j=-\begin{bmatrix}0\\ 0\\ n\end{bmatrix}
\tag{6.2.22}
$$

> 同样体现等大反向：刚体 $i$ 受 $+n$、刚体 $j$ 受 $-n$。

---

## 7. 小结

| 力元件 | 内力/力矩 | 广义力 |
|---|---|---|
| 一般作用力 $\mathbf{F}^P$ | 给定 | $\mathbf{Q}=[\mathbf{F}^{P};\ \mathbf{s}'^{P\,T}\mathbf{B}_i^{T}\mathbf{F}^{P}]$（6.2.3） |
| 体固连力 $\mathbf{F}'^P$ | 给定 | $\mathbf{Q}=[\mathbf{A}_i;\ (\mathbf{R}\mathbf{s}'^{P})^{T}]\mathbf{F}'^{P}$（6.2.4） |
| 平动弹簧–阻尼–作动器 | $f=k(\ell-\ell_0)+c\dot\ell+F$（6.2.10） | $\mathbf{Q}_{i,j}=\pm\frac{f}{\ell}[\mathbf{d}_{ij};\ \mathbf{d}_{ij}^{T}\mathbf{B}_{i,j}\mathbf{s}'_{i,j}]$（6.2.14） |
| 旋转弹簧–阻尼–作动器 | $n=k_\theta(\theta_{ij}-\theta_0)+c_\theta\dot\theta_{ij}+N$（6.2.18） | $\mathbf{Q}_{i,j}=\pm[0,0,n]^{T}$（6.2.22） |

本节定义的虚功与广义力，将在 §6.3 中用于组装受约束多体系统的变分运动方程。
