---
type: chapter-notes
parent: computer-aided-kinematics-and-dynamics
chapter: 6
title: "6.4 Inverse Dynamics of Kinematically Driven Systems"
pages: 229-230
sections:
  - "6.4 Inverse Dynamics of Kinematically Driven Systems"
created: 2026-06-18
last_updated: 2026-06-18
---

# 6.4 运动学驱动系统的逆动力学

> 来源：*Computer Aided Kinematics and Dynamics of Mechanical Systems, Vol. 1*，第 6 章，6.4 节（书页 229–230）。

---

## 前言

第 6.3 节得到的混合微分-代数方程（6.3.18）对**任意自由度**的约束系统都成立。

当**驱动约束（driving constraint）的数目恰好等于运动学自由度**时，整个系统是**运动学确定的（kinematically determined）**——位置、速度、加速度由第 3 章的位置/速度/加速度方程唯一确定，无需积分。此时雅可比 $\boldsymbol{\Phi}_{\mathbf{q}}$ 是**方阵且非奇异**：

$$\lvert\boldsymbol{\Phi}_{\mathbf{q}}(\mathbf{q},t)\rvert\neq 0 \tag{6.4.1}$$

由于运动已被规定，这里的目标不是"求运动"，而是**反过来**问：要让系统按规定的运动走，**关节里需要多大的反力/力矩**？这就是 **逆动力学（inverse dynamics）** 的含义——给定运动，反推为之提供动力的约束力（拉格朗日乘子（Lagrange multiplier））。

---

## 6.4.1 简化的运动方程

把 (6.3.18) 拆开写成两部分。第一部分是动力学方程，第二部分是加速度约束方程：

$$\mathbf{M}\ddot{\mathbf{q}} + \boldsymbol{\Phi}_{\mathbf{q}}^T\boldsymbol{\lambda} = \mathbf{Q}^A \tag{6.4.2}$$

$$\boldsymbol{\Phi}_{\mathbf{q}}\ddot{\mathbf{q}} = \boldsymbol{\gamma} \tag{6.4.3}$$

在逆动力学的情形下，$\boldsymbol{\Phi}_{\mathbf{q}}$ 是方阵且可逆，于是 (6.4.3) **本身就构成一个线性方程组**——加速度可以"独立地"先解出来。

### 解加速度

由 (6.4.3) 直接两边左乘 $\boldsymbol{\Phi}_{\mathbf{q}}^{-1}$：

$$\ddot{\mathbf{q}} = \boldsymbol{\Phi}_{\mathbf{q}}^{-1}\boldsymbol{\gamma} \tag{6.4.4}$$

> 实际计算中并不会真的去求 $\boldsymbol{\Phi}_{\mathbf{q}}^{-1}$；而是用第 4.4 节的高斯消元/L–U 分解直接数值求解 (6.4.3)。

### 解乘子

加速度已知后，把 $\ddot{\mathbf{q}}$ 代回 (6.4.2)：

$$\boldsymbol{\Phi}_{\mathbf{q}}^T\boldsymbol{\lambda} = \mathbf{Q}^A - \mathbf{M}\ddot{\mathbf{q}}$$

注意非奇异方阵的转置仍非奇异，故 $\boldsymbol{\Phi}_{\mathbf{q}}^T$ 也可逆。两边左乘 $(\boldsymbol{\Phi}_{\mathbf{q}}^T)^{-1}=\boldsymbol{\Phi}_{\mathbf{q}}^{-T}$：

$$\boxed{\boldsymbol{\lambda} = \boldsymbol{\Phi}_{\mathbf{q}}^{-T}\bigl[\mathbf{Q}^A - \mathbf{M}\ddot{\mathbf{q}}\bigr]} \tag{6.4.5}$$

### 为什么叫"逆动力学"

由 6.6 节将证明：拉格朗日乘子 $\boldsymbol{\lambda}$ **唯一**地决定了系统中各关节处的约束反力与反力矩。流程如下：

1. 工程师给定希望的驱动约束（即希望的运动轨迹）；
2. 解 (6.4.3) 得 $\ddot{\mathbf{q}}$（运动学层面已确定）；
3. 解 (6.4.2) 得 $\boldsymbol{\lambda}$；
4. 用 6.6 节的反力公式把 $\boldsymbol{\lambda}$ 翻译成实际作动器需提供的力/力矩。

这个流程"由运动反推力"，与正动力学"由力推运动"方向相反——故名 *inverse dynamics*。

---

## 例 6.4.1：单摆的逆动力学

> 沿用例 6.3.1、6.3.4、6.3.6 的单摆，并附加驱动约束（driving constraint）
> $$\Phi^D = \phi_1 - \tfrac{3\pi}{2} - 2\pi t = 0$$
> 即令杆以角速度 $\dot\phi_1 = 2\pi$ 匀速旋转。

![图 6.3.1 单摆](figures/computer-aided-kinematics-and-dynamics/fig_6_3_1_simple_pendulum.png)

### 约束方程

在 (6.3.20) 的基础上把驱动约束作为第三行加入。这里 $\mathbf{q}=[x_1,y_1,\phi_1]^T$，$\mathbf{M}=\operatorname{diag}(m,m,m\ell^2/3)$（杆质量 $m$，长 $2\ell$，质心极转动惯量 $J'=m\ell^2/3$）。

运动学约束：
$$\Phi_1 = x_1 - \ell\cos\phi_1 = 0,\qquad \Phi_2 = y_1 - \ell\sin\phi_1 = 0$$

驱动约束：
$$\Phi^D = \phi_1 - \tfrac{3\pi}{2} - 2\pi t = 0$$

对 $\mathbf{q}=(x_1,y_1,\phi_1)$ 求偏导：
$$\boldsymbol{\Phi}_{\mathbf{q}} = \begin{bmatrix}
1 & 0 & \ell\sin\phi_1 \\
0 & 1 & -\ell\cos\phi_1 \\
0 & 0 & 1
\end{bmatrix},\qquad \boldsymbol{\Phi}_{\mathbf{q}}^T = \begin{bmatrix}
1 & 0 & 0 \\
0 & 1 & 0 \\
\ell\sin\phi_1 & -\ell\cos\phi_1 & 1
\end{bmatrix}$$

外加力只有重力 $\mathbf{Q}^A=[0,-mg,0]^T$。

### 加速度方程


按 3.1.10 的通用公式 $\boldsymbol{\gamma} = -(\boldsymbol{\Phi}_\mathbf{q}\dot{\mathbf{q}})_\mathbf{q}\dot{\mathbf{q}} - 2\boldsymbol{\Phi}_{\mathbf{q}t}\dot{\mathbf{q}} - \boldsymbol{\Phi}_{tt}$ 。先算第一项：

$$
(\boldsymbol{\Phi}_\mathbf{q}\dot{\mathbf{q}})_\mathbf{q}\dot{\mathbf{q}}
= \left(
\begin{bmatrix}
1 & 0 & \ell\sin\phi_1 \\
0 & 1 & -\ell\cos\phi_1 \\
0 & 0 & 1
\end{bmatrix}
\begin{bmatrix}\dot x_1\\\dot y_1\\\dot\phi_1\end{bmatrix}
\right)_{\!\mathbf{q}}\dot{\mathbf{q}}
= \begin{bmatrix}\dot x_1+\ell\sin\phi_1\,\dot\phi_1\\ \dot y_1-\ell\cos\phi_1\,\dot\phi_1\\ \dot\phi_1\end{bmatrix}_{\!\mathbf{q}}\dot{\mathbf{q}}
= \begin{bmatrix}
0 & 0 & \ell\cos\phi_1\,\dot\phi_1 \\
0 & 0 & \ell\sin\phi_1\,\dot\phi_1 \\
0 & 0 & 0
\end{bmatrix}
\begin{bmatrix}\dot x_1\\\dot y_1\\\dot\phi_1\end{bmatrix}
= \begin{bmatrix}\ell\cos\phi_1\,\dot\phi_1^2\\ \ell\sin\phi_1\,\dot\phi_1^2\\ 0\end{bmatrix}
$$

由于 $\boldsymbol{\Phi}_\mathbf{q}$ 各元素都不显含 $t$，且只有驱动约束 $\Phi^D$ 线性含 $t$，故含时间偏导的两项全为零：

$$\boldsymbol{\Phi}_{\mathbf{q}t}=\mathbf{0}_{3\times3},\qquad \boldsymbol{\Phi}_{tt}=\mathbf{0}_{3\times1}$$

因此得到加速度方程的右边项：

$$\boldsymbol{\gamma}
= -\begin{bmatrix}\ell\cos\phi_1\,\dot\phi_1^2\\ \ell\sin\phi_1\,\dot\phi_1^2\\ 0\end{bmatrix}
- 2\,\mathbf{0}_{3\times3}\,\dot{\mathbf{q}}
- \mathbf{0}_{3\times1}
= \begin{bmatrix}-\ell\cos\phi_1\,\dot\phi_1^2\\ -\ell\sin\phi_1\,\dot\phi_1^2\\ 0\end{bmatrix}$$

### 运动方程组

联立得到：

$$\begin{bmatrix} m & 0 & 0\\ 0 & m & 0\\ 0 & 0 & \tfrac{m\ell^2}{3} \end{bmatrix}\!\begin{bmatrix}\ddot x_1\\ \ddot y_1\\ \ddot\phi_1\end{bmatrix} + \begin{bmatrix} 1 & 0 & 0\\ 0 & 1 & 0\\ \ell\sin\phi_1 & -\ell\cos\phi_1 & 1 \end{bmatrix}\!\begin{bmatrix}\lambda_1\\\lambda_2\\\lambda_3\end{bmatrix} = \begin{bmatrix} 0\\ -mg\\ 0\end{bmatrix} \tag{6.4.6}$$

$$\begin{bmatrix}
1 & 0 & \ell\sin\phi_1 \\
0 & 1 & -\ell\cos\phi_1 \\
0 & 0 & 1
\end{bmatrix}\!\begin{bmatrix}\ddot x_1\\\ddot y_1\\\ddot\phi_1\end{bmatrix} = -\begin{bmatrix}\ell\cos\phi_1\,\dot\phi_1^2\\ \ell\sin\phi_1\,\dot\phi_1^2\\ 0\end{bmatrix} \tag{6.4.7}$$

### 解 (6.4.7) 求加速度

易得：

$$\begin{bmatrix}\ddot x_1\\\ddot y_1\\\ddot\phi_1\end{bmatrix} = \begin{bmatrix}-\ell\cos\phi_1\,\dot\phi_1^2\\ -\ell\sin\phi_1\,\dot\phi_1^2\\ 0\end{bmatrix} \tag{6.4.8}$$

### 把 $\ddot{\mathbf{q}}$ 代回 (6.4.6) 求拉格朗日乘子

这一部分也容易得到：

$$\boxed{\begin{bmatrix}\lambda_1\\\lambda_2\\\lambda_3\end{bmatrix} = \begin{bmatrix} m\ell\cos\phi_1\,\dot\phi_1^2\\ -mg + m\ell\sin\phi_1\,\dot\phi_1^2\\ -mg\ell\cos\phi_1\end{bmatrix}} \tag{6.4.9}$$

### 物理解读

虽然一般情形下要用 6.6 节的反力公式把乘子翻译成关节力，但这里乘子的物理意义已可直读：

- $\lambda_1$：作用于摆质心的 **$x$ 方向向心力**（指向悬点的水平分量）。
- $\lambda_2$：**$y$ 方向向心力 减 重力**——即除重力外，悬点向上拉摆的竖直分量（既要提供竖直向心力，又要平衡 $-mg$）。
- $\lambda_3$：与驱动约束对应，等于 $-mg\ell\cos\phi_1$，即**重力关于悬点的力矩**——这正是要让杆以恒定 $\dot\phi_1$ 旋转所需的驱动力矩（重力试图减/加速摆，作动器须正好抵消其力矩贡献）。

> 验证：当 $\phi_1=\pi/2$（杆竖直向上）或 $3\pi/2$（向下）时 $\cos\phi_1=0$，重力沿杆方向、对悬点无力矩，故 $\lambda_3=0$。符合直觉。
